import React from 'react';
import './App.css'; // Ensure this file exists in the project directory

const App = () => {
  return (
    <div className="container">
      <h1 className="title">Welcome to My Styled App</h1>
      <p className="description">This is a demo of CSS styling in React.</p>
    </div>
  );
};

export default App;



App.css

.container {
  text-align: center;
  margin-top: 50px;
}

.title {
  color: blue;
  font-size: 2rem;
}

.description {
  color: gray;
  font-size: 1.2rem;
}
